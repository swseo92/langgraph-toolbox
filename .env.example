# API Keys (테스트 및 개발용)
# 주의: 절대로 .env를 git에 커밋하지 마세요!
# .env는 .gitignore에 포함되어 자동으로 제외됩니다.

# OpenAI API Key (LLM 호출용)
# https://platform.openai.com/account/api-keys에서 발급받으세요
OPENAI_API_KEY=sk-

# Anthropic API Key (선택사항, Claude 사용시)
# https://console.anthropic.com/account/keys에서 발급받으세요
ANTHROPIC_API_KEY=sk-ant-

# 기타 API Keys (필요시)
# GOOGLE_API_KEY=
# GITHUB_TOKEN=

# 환경 설정
# TEST_MODE는 conftest.py에서 자동으로 'true'로 설정됩니다
# 필요시 다른 모드로 override할 수 있습니다
# TEST_MODE=false

# 로깅 레벨
# LOG_LEVEL=INFO

# VCR Cassette 설정 (테스트용 API 응답 기록)
# 첫 실행 시 실제 API 호출 → cassette에 저장
# 이후 실행 시 cassette에서 재생 (API 호출 없음)
# VCR_RECORD_MODE=once

# 프록시 설정 (필요시)
# HTTP_PROXY=
# HTTPS_PROXY=

# 데이터베이스 URL (필요시)
# DATABASE_URL=

# 기타 환경 변수 추가하기
# 템플릿: VARIABLE_NAME=value
